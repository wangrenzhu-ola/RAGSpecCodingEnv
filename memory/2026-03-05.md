# Daily Memory: 2026-03-05

### Hybrid先行+按需Graph扩展需求与方案草案 (10:12:31)
- 目标：默认走Hybrid检索，检测多跳/因果/跨实体问题时自动升级到Graph扩展层。
- 切入点：memory_search工具执行链在 src/agents/tools/memory-tool.ts 调 manager.search。
- 核心改造：新增query router配置与路由器模块，按意图与检索信号决定hybrid_only或hybrid_plus_graph。
- 成本预估：MVP约1-2周（规则路由+本地图近邻），完整图谱版约4-8周（抽取、建图、增量更新、评测）。
- 风险：抽取质量与索引延迟；需通过降级到Hybrid与离线回放评测控制。

### Graph扩展改造Token预算评估 (10:27:13)
- 用户要求改为评估全自动改造的Token消耗，不交给人工。
- 估算：MVP约80万-180万tokens；增强版约180万-350万tokens；完整生产版约350万-700万tokens。
- 估算口径包含对话上下文、代码检索、补丁、测试回归与反复调参重试。
- 建议先做MVP验证再按效果决定是否投入完整图谱阶段。

### 整合当前记忆-校验 (11:35:35)
- [当前上下文] 决策: 默认只开routing
- [当前上下文] 行动: graph按阈值触发
- [当前上下文] 风险: 阈值需要继续调优
- [当前上下文] 关键词聚合：决策, 默认只开routing, 行动, graph按阈值触发, 风险

### 整合当前记忆-结构化标签校验 (12:21:31)
- [当前上下文][kind=decision][scope=session][status=active][topic=routing] 决策: 默认只开routing
- [当前上下文][kind=action][scope=session][status=active][topic=graph] 行动: graph按阈值触发
- [当前上下文][kind=risk][scope=session][status=active][topic=misc] 风险: 阈值需要继续调优
- [当前上下文][kind=constraint][scope=session][status=active][topic=misc] 约束: 必须可回滚
- [当前上下文][kind=decision][scope=session][status=active][topic=routing] 关键词聚合：决策, 默认只开routing, 行动, graph按阈值触发, 风险

### 整合当前记忆 (12:27:41)
- [当前会话][kind=decision][scope=project][status=active][topic=schema] 决策: consolidate 已接入4层受控标签(kind/scope/status/topic)
- [当前会话][kind=action][scope=project][status=active][topic=schema] 行动: 已实现自动映射(kind/topic)与阈值校验(unknown_topic_ratio,fallback_kind_ratio)
- [当前会话][kind=action][scope=project][status=active][topic=schema] 行动: 新增参数 scope/status/default-topic 与 allow-threshold-breach
- [当前会话][kind=action][scope=project][status=active][topic=schema] 行动: 已补充单测并通过本地校验(7 tests)
- [当前会话][kind=risk][scope=project][status=active][topic=schema] 风险: topic 仍可能落入 misc 需持续扩充关键词词表
- [当前会话][kind=action][scope=project][status=active][topic=schema] 关键词聚合：topic, 行动, kind, scope, status

### HKT-memory v3 更新文档完成 (12:32:03)
- 按 v2 风格新增 HKT-memory-v3-release.md，覆盖 routing + graph + consolidate 更新点
- 文档新增 v3 架构图、Write/Query/Consolidate 时序图、关键默认参数与 show-mode 示例
- 示例查询显示 Mode: hybrid_plus_graph，用于体现 v3 路由能力

### README置顶v3摘要并修复Mermaid (13:20:52)
- 已在仓库README顶部新增HKT-memory v3更新速览，确保一眼可见
- 已修复 HKT-memory-v3-release.md 架构图节点文本中的 Mermaid 解析错误：将 vertical bar 改为斜杠
- 文档链接与摘要内容已校对，诊断检查无新增问题
